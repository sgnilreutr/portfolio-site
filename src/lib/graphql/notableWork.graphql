query GetNotableWork($id: String!) {
  notableWork(id: $id) {
    internalName
    notableWorkListCollection {
      items {
        ... on NotableItem {
          label
          notableListItem
        }
      }
    }
  }
}
